<manifest xmlns:android="http://schemas.android.com/apk/res/android" xmlns:tools="http://schemas.android.com/tools">
  <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>
  <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
  <uses-permission android:name="android.permission.CAMERA"/>
  <uses-permission android:name="android.permission.INTERNET"/>
  <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" android:maxSdkVersion="32"/>
  <uses-permission android:name="android.permission.RECORD_AUDIO" tools:node="remove"/>
  <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" tools:node="remove"/>
  <uses-permission android:name="android.permission.VIBRATE"/>
  <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" tools:node="remove"/>
  <queries>
    <intent>
      <action android:name="android.intent.action.VIEW"/>
      <category android:name="android.intent.category.BROWSABLE"/>
      <data android:scheme="https"/>
    </intent>
  </queries>
  <application android:name=".MainApplication" android:label="@string/app_name" android:icon="@mipmap/ic_launcher" android:allowBackup="true" android:theme="@style/AppTheme" android:roundIcon="@mipmap/ic_launcher_round">
    <!-- OTA UPDATES OFF, AND THIS FILE IS WHERE THAT DECISION LIVES.
         Setting `updates.enabled: false` in app.json does NOT reach Android in
         this project: android/ is committed source (bare workflow), so the
         manifest is not regenerated from the app config. Same class of staleness
         as the createExpoConfig note in app/build.gradle — and it was measured,
         not assumed: with app.json already saying false, aapt2 showed
         `expo.modules.updates.ENABLED=true` in the built APK and logcat showed 43
         failed update checks in one launch.

         WHY OFF. EAS Update is not actually in use — no channel is configured, so
         every check fails with `"channel-name": Required`. It buys nothing and
         costs this: with CHECK_ON_LAUNCH=ALWAYS and LAUNCH_WAIT_MS=0, the app
         launches the CACHED JS bundle and only applies a newer one on the NEXT
         launch. Every sideloaded field APK therefore ran the PREVIOUS build's
         JavaScript on first open — a fix would appear missing until the app was
         opened twice, which cost real field time diagnosing bugs that were
         already fixed.

         To re-enable: set this to true, configure a channel, and keep
         runtimeVersion tied to appVersion so a stale bundle cannot be reused. -->
    <meta-data android:name="expo.modules.updates.ENABLED" android:value="false"/>
    <meta-data android:name="expo.modules.updates.EXPO_RUNTIME_VERSION" android:value="@string/expo_runtime_version"/>
    <meta-data android:name="expo.modules.updates.EXPO_UPDATES_CHECK_ON_LAUNCH" android:value="ALWAYS"/>
    <meta-data android:name="expo.modules.updates.EXPO_UPDATES_LAUNCH_WAIT_MS" android:value="0"/>
    <meta-data android:name="expo.modules.updates.EXPO_UPDATE_URL" android:value="https://u.expo.dev/d24307ed-5428-4bbc-97ba-efd4a426856b"/>
    <activity android:name=".MainActivity" android:configChanges="keyboard|keyboardHidden|orientation|screenSize|screenLayout|uiMode|locale|layoutDirection" android:launchMode="singleTask" android:windowSoftInputMode="adjustResize" android:theme="@style/Theme.App.SplashScreen" android:exported="true" android:screenOrientation="portrait">
      <intent-filter>
        <action android:name="android.intent.action.MAIN"/>
        <category android:name="android.intent.category.LAUNCHER"/>
      </intent-filter>
      <intent-filter>
        <action android:name="android.intent.action.VIEW"/>
        <category android:name="android.intent.category.DEFAULT"/>
        <category android:name="android.intent.category.BROWSABLE"/>
        <data android:scheme="gemscanai"/>
        <data android:scheme="com.gemscan.ai"/>
      </intent-filter>
    </activity>
    <activity android:name="com.facebook.react.devsupport.DevSettingsActivity" android:exported="false"/>
  </application>
</manifest>